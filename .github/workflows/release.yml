name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            cv_arch: darwin-arm64
            asset_suffix: darwin-arm64
          - os: ubuntu-latest
            cv_arch: linux-amd64
            asset_suffix: linux-amd64
          - os: ubuntu-24.04-arm
            cv_arch: linux-arm64
            asset_suffix: linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install cv
        run: |
          CV_VERSION=$(gh release view --repo marcelocantos/cv --json tagName -q .tagName)
          CV_VER=${CV_VERSION#v}
          curl -fsSL "https://github.com/marcelocantos/cv/releases/download/${CV_VERSION}/cv-${CV_VER}-${{ matrix.cv_arch }}.tar.gz" \
            | tar xz
          sudo mv cv /usr/local/bin/cv
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libreadline-dev

      - name: Run tests
        run: cv test

      - name: Build binary
        run: cv shell

      - name: Package
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"
          TARBALL="sqldeep-${VERSION}-${{ matrix.asset_suffix }}.tar.gz"
          tar czf "$TARBALL" -C build sqldeep
          echo "TARBALL=$TARBALL" >> "$GITHUB_ENV"

      - name: Upload to release
        run: gh release upload "${{ github.event.release.tag_name }}" "${{ env.TARBALL }}"
        env:
          GH_TOKEN: ${{ github.token }}

  homebrew:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: Justintime50/homebrew-releaser@v3
        with:
          homebrew_owner: marcelocantos
          homebrew_tap: homebrew-tap
          github_token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          formula_folder: Formula
          version: ${{ github.event.release.tag_name }}
          install: 'bin.install "sqldeep" => "sqldeep"'
          target_darwin_arm64: true
          target_linux_amd64: true
          target_linux_arm64: true
          test: 'system bin/"sqldeep", "--version"'
          skip_checksum: true
          update_readme_table: true
